const FTMLParser = {
  /**
   * MDParser.parse()
   *
   * method that converts markdown to FTML
   */
  parse: ftml => {
    const parsed = ftml;

    return parsed;
  },
};

export default FTMLParser;
